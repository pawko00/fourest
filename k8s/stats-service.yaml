# Stats Service Kubernetes Deployment
apiVersion: v1
kind: ConfigMap
metadata:
  name: stats-service-config
  namespace: focusforest
data:
  NODE_ENV: production
  PORT: "3003"
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_USERNAME: focusforest
  DB_DATABASE: focusforest
  CORS_ORIGIN: "*"
---
apiVersion: v1
kind: Secret
metadata:
  name: stats-service-secret
  namespace: focusforest
type: Opaque
stringData:
  DB_PASSWORD: focusforest123
  JWT_SECRET: your-super-secret-jwt-key-change-in-production
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stats-service
  namespace: focusforest
  labels:
    app: stats-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stats-service
  template:
    metadata:
      labels:
        app: stats-service
    spec:
      serviceAccountName: stats-service-sa
      containers:
        - name: stats-service
          image: focusforest-stats-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3003
          envFrom:
            - configMapRef:
                name: stats-service-config
            - secretRef:
                name: stats-service-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: stats-service
  namespace: focusforest
spec:
  selector:
    app: stats-service
  ports:
    - port: 3003
      targetPort: 3003
  type: ClusterIP
